<UserControl x:Class="CaelestiaWin.UI.Views.ClipboardHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Border Width="420"
            MaxHeight="560"
            Margin="0,0,0,42"
            Padding="18"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            RenderTransformOrigin="0.5,1">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform ScaleX="1" ScaleY="1" />
                <TranslateTransform Y="0" />
            </TransformGroup>
        </Border.RenderTransform>
        <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource GlassPanelStyle}">
                <Setter Property="Visibility" Value="Collapsed" />
                <Setter Property="Opacity" Value="0" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOpen}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0"
                                                     To="1"
                                                     Duration="0:0:0.14" />
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                                     From="0.96"
                                                     To="1"
                                                     Duration="0:0:0.18">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TransformGroup.Children)[1].(TranslateTransform.Y)"
                                                     From="18"
                                                     To="0"
                                                     Duration="0:0:0.18">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Clipboard"
                               FontFamily="{DynamicResource ShellHeadingFont}"
                               FontSize="24"
                               Foreground="{DynamicResource ShellForegroundBrush}" />
                    <TextBlock Text="Recent text, images, and files copied while Nebula is running"
                               Margin="0,5,0,0"
                               Foreground="{DynamicResource ShellMutedBrush}" />
                </StackPanel>
                <Button Grid.Column="1"
                        Width="38"
                        Height="38"
                        Margin="0,0,8,0"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding ClearCommand}"
                        Content="&#xE74D;"
                        ToolTip="Clear history" />
                <Button Grid.Column="2"
                        Width="38"
                        Height="38"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding CloseCommand}"
                        Content="&#xE711;"
                        ToolTip="Close" />
            </Grid>

            <Grid Grid.Row="1">
                <TextBlock Text="Copy text, images, or files and they will appear here."
                           TextAlignment="Center"
                           Margin="18,44"
                           Foreground="{DynamicResource ShellMutedBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasItems}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <ScrollViewer MaxHeight="420"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Margin="0,0,0,10"
                                        Padding="14"
                                        HorizontalContentAlignment="Stretch"
                                        Command="{Binding CopyCommand}"
                                        Style="{StaticResource ShellButtonStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <Border Width="40"
                                                    Height="40"
                                                    CornerRadius="12"
                                                    Background="#10000000"
                                                    VerticalAlignment="Top">
                                                <Grid>
                                                    <Image Source="{Binding ImagePath}"
                                                           Stretch="UniformToFill"
                                                           Visibility="{Binding HasImagePreview, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                    <TextBlock Text="{Binding Glyph}"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="18"
                                                               Foreground="{DynamicResource AccentBrush}"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasImagePreview}" Value="True">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>

                                            <StackPanel Grid.Column="1"
                                                        Margin="12,0,0,0">
                                                <TextBlock Text="{Binding Title}"
                                                           Foreground="{DynamicResource ShellForegroundBrush}"
                                                           FontFamily="{DynamicResource ShellHeadingFont}"
                                                           FontSize="14"
                                                           TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding SecondaryText}"
                                                           Margin="0,4,0,0"
                                                           Foreground="{DynamicResource ShellMutedBrush}"
                                                           FontSize="12"
                                                           TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Grid.Row="1"
                                                   Margin="0,12,0,0"
                                                   Text="{Binding Preview}"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="88"
                                                   Foreground="{DynamicResource ShellForegroundBrush}" />
                                        <TextBlock Grid.Row="2"
                                                   Text="{Binding Timestamp}"
                                                   Margin="0,9,0,0"
                                                   HorizontalAlignment="Right"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource ShellMutedBrush}" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</UserControl>
